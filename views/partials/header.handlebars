<header class="dark_bg">
    <nav id="nav-main">
        <a href="/" class="logo">Keikalle.com</a>
        <div id="sign-in-info">
            {{#if alias}}
                <p style="display: inline;">Olet kirjautunut sisään: {{alias}}</p>
                <button type="button" id="sign-out-btn" class="sign-out-btn" onclick="location.href='/logout'">Kirjaudu ulos</button>
                <a href="/profile" class="nav-link">Profiili</a>
            {{/if}}
        </div>
    </nav>
    <!-- {{#if userPressesLoginButtonShowThis}}
    {{>loginform}}
    {{/if}} -->
</header> 